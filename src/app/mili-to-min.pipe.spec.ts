import { MiliToMinPipe } from './mili-to-min.pipe';

describe('MiliToMinPipe', () => {
  it('create an instance', () => {
    const pipe = new MiliToMinPipe();
    expect(pipe).toBeTruthy();
  });
});
